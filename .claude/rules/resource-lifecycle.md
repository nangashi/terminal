---
paths:
  - "src-tauri/src/**/*.rs"
---

## リソースのライフサイクル管理

PTY の子プロセス (`Child`) やリーダースレッド等、OS リソースを生成した場合は終了検知とクリーンアップを必ず実装する。

- `spawn_command` の戻り値 (`Child`) を捨てない — プロセス終了検知に必要
- リーダースレッド終了時は対応リソースをマップから削除し、フロントエンドに通知する
- リソース解放漏れはメモリ/プロセスリークに直結する
